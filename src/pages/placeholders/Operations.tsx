import { Workflow } from "lucide-react";
import SectionPlaceholder from "./SectionPlaceholder";

export default function Operations() {
  return <SectionPlaceholder title="Operations" batch="Batch 3" icon={Workflow} />;
}
