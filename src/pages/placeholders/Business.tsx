import { Briefcase } from "lucide-react";
import SectionPlaceholder from "./SectionPlaceholder";

export default function Business() {
  return <SectionPlaceholder title="Business" batch="Batch 4" icon={Briefcase} />;
}
