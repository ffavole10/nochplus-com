import { BookOpen } from "lucide-react";
import SectionPlaceholder from "./SectionPlaceholder";

export default function Knowledge() {
  return <SectionPlaceholder title="Knowledge" batch="Batch 5" icon={BookOpen} />;
}
