import { Target } from "lucide-react";
import SectionPlaceholder from "./SectionPlaceholder";

export default function CommandCenter() {
  return <SectionPlaceholder title="Command Center" batch="Batch 2" icon={Target} />;
}
